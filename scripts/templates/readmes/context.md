# qa/context/

The only directory you populate manually. Drop any prior knowledge here before discovery.

Accepts: `.md`, `.txt`, `.png`, `.jpg`. The agent reads everything in this directory in Step 3.

- `feature-specs/` — PRDs, product specs, GitHub notes
- `figma-screens/` — Figma exports, design mocks, app screenshots
